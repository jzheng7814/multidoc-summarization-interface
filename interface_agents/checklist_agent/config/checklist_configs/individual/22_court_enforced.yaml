# Checklist configuration - Whether the Settlement is Court-enforced or Not only
# Focuses exclusively on extracting the court enforcement status

# User instruction
user_instruction: |
  Extract the court enforcement status from the legal documents.
# Task-specific constraints
constraints:
  - "Extract EVERY distinct value of this checklist item found in any document."
  - "Create separate entries for each unique value with its supporting evidence."
  - "Each extracted value should be self-contained with relevant context included."
# Single checklist item
checklist_items:
  Whether_the_Settlement_is_Court-enforced_or_Not:
    description: "Whether a settlement (not entered as an order/judgment) is court-enforced. Answer 'Yes' if the court explicitly retains jurisdiction to enforce the settlement without incorporating it into an order or judgment (e.g., Kokkonen retention), and 'No' if it is a private agreement with no retained jurisdiction. The extracted value should be formatted as '[Yes/No].'"
